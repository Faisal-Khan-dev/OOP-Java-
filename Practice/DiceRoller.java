// import java.util.Random;

// public class DiceRoller {

//     Random random;      // now these are known as globle variables
//     int num;
//     DiceRoller() {

//         random = new Random();

//         // Random random = new Random(); //instance of this obj is an local scope variable
//         // int num = 0;        // also the num is an local scope variable
//         // roll(random , num);         // we can't access it directly so we pass it as an argument
//         roll();         
//     }

//     void roll() {
//         num = random.nextInt(6) + 1;
//         System.out.println(num);
//     }

// }

